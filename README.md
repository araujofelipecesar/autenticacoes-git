# Autenticacoes-git
Repositório criado para o curso de princípios do git-github da DIO

* Autenticação via nome de usuário (descontinuada).
* Atenticação via token de acesso pessoal.

* Autenticação via SSH.

​	Testando a troca de URL de token para chave SSH.
